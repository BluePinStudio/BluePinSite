source "https://rubygems.org"

# Use GitHub Pages’ pinned dependency set (includes Jekyll 3.10 and allowed plugins)
gem "github-pages", "~> 232", group: :jekyll_plugins

# Optional but handy for local `jekyll serve` on newer Ruby
group :development do
  gem "webrick", "~> 1.8"
end
